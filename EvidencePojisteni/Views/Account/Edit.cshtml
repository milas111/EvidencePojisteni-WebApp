@model EvidencePojisteni.Models.Insureds.InsuredBase
@{
    ViewData["Title"] = "Změna údajů";
}

<h3 class="font-weight-bold my-4">Úprava osobních údajů</h3>

<form asp-action="Edit" method="post">

    <partial name="_InsuredPartial" />
    <div class="text-center my-4">
        <button type="submit" class="btn-lg btn-info">Uložit</button>
    </div>
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}